const leia = require('readline-sync')

//let vetorStrings = [" Boxer", "Pastor Alemão", "Pischer", "Husky Siberiano", "Corgi"]

let vetorInteiro  =[]

for (let contador = 0; contador < vetorStrings.length; contador ++){

}

console.log(vetorStrings[contador])
// impreme o vetor em forma de uma tabela console.table()
// .sort para ordenar os dados so classifica string

// funcao de ajuste sorte((a,b) => a-b)) ajuste ao inves de ler posição por posicoa string le como um todo